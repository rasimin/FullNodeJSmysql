{
  "name": "full-node-mysql-project",
  "version": "1.0.0",
  "description": "Unified runner for Backend and Frontend",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --host 0.0.0.0\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend -- --host 0.0.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
